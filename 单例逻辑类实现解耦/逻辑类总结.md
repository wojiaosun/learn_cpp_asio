# 单例逻辑类实现解耦合

> 一些细节点

## 单例类
单例模式有很多种写法，其中也设计线程安全的问题，这里用**C11特性once_flag和call_once**一起使用，以后无论调用多少次，因为s_flag是静态局部变量，所以都不会被重复初始化，call_once只会调用一次，而且线程安全，原理其实也是加锁，第一次调用会执行lambda表达式，第二次调用call_once判断s_flag的值就不会初始化智能指针了。

```cpp
template <typename T>
class Singleton {
protected:
	Singleton() = default;
	Singleton(const Singleton<T>&) = delete;
	Singleton& operator=(const Singleton<T>& st) = delete;
	
	static std::shared_ptr<T> _instance;
public:
	static std::shared_ptr<T> GetInstance() {
		static std::once_flag s_flag;
		std::call_once(s_flag, [&]() {
			_instance = shared_ptr<T>(new T);
			});

		return _instance;
	}
	...
};

template <typename T>
std::shared_ptr<T> Singleton<T>::_instance = nullptr;

```
## 逻辑类


### 处理消息函数
初始化一个unique_lock，主要是为了控制队列的安全，配合条件变量可以随时解锁，lock_guard不可以解锁。
```cpp
void LogicSystem::DealMsg() {
    for (;;) {
        std::unique_lock<std::mutex> unique_lk(_mutex);
        //判断队列为空则用条件变量阻塞等待，并释放锁
        while (_msg_que.empty() && !_b_stop) {
            _consume.wait(unique_lk);
        }
        //判断是否为关闭状态，把所有逻辑执行完后则退出循环
        if (_b_stop ) {
            while (!_msg_que.empty()) {
                auto msg_node = _msg_que.front();
                cout << "recv_msg id  is " << msg_node->_recvnode->_msg_id << endl;
                auto call_back_iter = _fun_callbacks.find(msg_node->_recvnode->_msg_id);
                if (call_back_iter == _fun_callbacks.end()) {
                    _msg_que.pop();
                    continue;
                }
                call_back_iter->second(msg_node->_session, msg_node->_recvnode->_msg_id,
                    std::string(msg_node->_recvnode->_data, msg_node->_recvnode->_cur_len));
                _msg_que.pop();
            }
            break;
        }
        //如果没有停服，且队列中有数据
        auto msg_node = _msg_que.front();
        cout << "recv_msg id  is " << msg_node->_recvnode->_msg_id << endl;
        auto call_back_iter = _fun_callbacks.find(msg_node->_recvnode->_msg_id);
        if (call_back_iter == _fun_callbacks.end()) {
            _msg_que.pop();
            continue;
        }
        call_back_iter->second(msg_node->_session, msg_node->_recvnode->_msg_id, 
            std::string(msg_node->_recvnode->_data, msg_node->_recvnode->_cur_len));
        _msg_que.pop();
    }
}
```

### 投递函数
来新消息唤醒工作线程
```cpp
void LogicSystem::PostMsgToQue(shared_ptr < LogicNode> msg) {
    std::unique_lock<std::mutex> unique_lk(_mutex);
    _msg_que.push(msg);
    //由0变为1则发送通知信号
    if (_msg_que.size() == 1) {
        _consume.notify_one();
    }
}
```
### 析构函数
这里为什么要_consume.notify_one()呢，因为需要等待工作线程处理完在退出，但是有可能工作线程可能处于挂起状态，这里就需要先发送一个激活信号唤醒工作线程。并且将_b_stop标记设置为true
```cpp
LogicSystem::~LogicSystem(){
    _b_stop = true;
    _consume.notify_one();
    _worker_thread.join();
}
```
